---
layout: null
sitemap: false
---
{%- assign back = page.dir | split: "/" -%}
{%- if back.size == 0 -%}
{%- assign location = include.location -%}
{%- else -%}
{%- include location.html -%}
{%- endif -%}
{%- assign realtor = page.name | remove: '.json' -%}
{%- assign data = site.data[realtor] | where_exp: 'i', 'i.location contains location' -%}
{%- assign offers = data | sort: 'date' | reverse -%}
{{ offers | jsonify }}
